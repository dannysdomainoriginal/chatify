{
  "name": "chatify-app",
  "version": "1.0.0",
  "description": "Realtime Chat App",
  "homepage": "https://github.com/dannysdomainoriginal/chatify#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dannysdomainoriginal/chatify.git"
  },
  "license": "ISC",
  "author": "Charles Daniel",
  "main": "backend/server.ts",
  "scripts": {
    "build": "npm i --prefix backend && npm i --prefix frontend && npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm run start --prefix backend"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "http-errors": "^2.0.1",
    "morgan": "^1.10.1",
    "prettier": "^3.7.4",
    "resend": "^6.6.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/cookie-parser": "^1.4.10",
    "@types/jsonwebtoken": "^9.0.10"
  }
}
